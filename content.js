console.log("no work we trade loaded")

const currentUrl = window.location.href

if (currentUrl.includes("pocketoption")) {
    console.log("pocket option detected")

    alert("no work we trade connected")
}
